<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.cyberspace_info">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:largeHeap="true"
        android:hardwareAccelerated="false"
        android:theme="@style/AppTheme">
        <activity android:name=".pesquisarimgvid.view.PesquisaActivity" />
        <activity android:name=".pesquisarimgvid.view.PesquisaImgVidActivity" />
        <activity android:name=".autenticacao.view.AutenticacaoActivity" android:theme="@style/AppTheme"/>
        <activity
            android:name=".planetasorbitandoestrelas.view.PlanetasOrbitandoEstrelasActivity"/>
        <activity
            android:name=".main.view.SplashScreenActivity"
            android:theme="@style/AppThemeSplash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".menu.view.MainActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>